<% if(msg != null ){ %>
    <div id="logout">
        <p>
            Bine ai venit, <%= msg %>!
        </p>
        <form action="/logout" method="post">
            <input type="submit" value="Logout">
        </form>
    </div>
    <% } %>
<div id="rez_chestionar">
<% if(p == cnt){ %>
    <p>felicitari, ai punctaj maxim! <%= ( p + ' din ' + cnt ) %> </p>
    
    <% }
else if(p < cnt && p != 0) { %>
    <p>Not great, not terrible! keep trying! <%= ( p + ' din ' + cnt ) %></p>
    <% }
else { %>
    <p>Total incorect... <%= ( p + ' din ' + cnt ) %></p>
    <% } %>
</div>
